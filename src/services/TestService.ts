import CardPrintingRepository from "../repositories/ICardPrintingRepository"
import CardPrinting from "../models/CardPrinting.model"

// Not used currently but was used to test endpoints and functionality
// Will likely delete later
export async function testPrintingDB(name: string) {
    
    //CardPrintingRepository.save()

}